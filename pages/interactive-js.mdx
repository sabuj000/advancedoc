import CodeBlock from '../components/CodeBlock';

# My Interactive Document

Here's an example of executing JavaScript code right within MDX:

<CodeBlock code={`const add = (a, b) => a + b; add(2, 3);`} />
