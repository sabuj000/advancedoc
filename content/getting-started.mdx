# Getting Started

This is a sample MDX page that demonstrates the usage of JSX components.

## Using Custom Components

Here's an example of using a custom Alert component:

<Alert type="info">
  This is an informational alert component rendered from MDX!
</Alert>

## Code Block Example

Here's a code block with syntax highlighting:

```javascript
const greeting = "Hello, MDX!";
console.log(greeting);
```

## API Documentation

<ApiRequest method="GET" endpoint="/api/users">
  This is a sample API request component
</ApiRequest>

<ApiResponse status={200}>
  {
    "data": {
      "users": [
        {
          "id": 1,
          "name": "John Doe"
        }
      ]
    }
  }
</ApiResponse> 